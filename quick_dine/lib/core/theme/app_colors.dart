import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF000000);
  static const Color secondary = Color(0xFF64748B);
  static const Color accent = Color(0xFFEF4444);
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color error = Color(0xFFDC2626);
  static const Color success = Color(0xFF059669);
  static const Color warning = Color(0xFFD97706);
  static const Color info = Color(0xFF0284C7);
  
  // Text colors
static const Color textPrimary = Color(0xFF000000); // Negro puro
static const Color textSecondary = Color(0xFF333333); // Gris oscuro
static const Color textMuted = Color(0xFF666666); // Gris medio

  
  // Border colors
  static const Color border = Color(0xFFE2E8F0);
  static const Color borderLight = Color(0xFFF1F5F9);
}
